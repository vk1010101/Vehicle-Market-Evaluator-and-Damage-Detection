services:
  - type: web
    name: car-scraper-app
    runtime: docker
    plan: free # Changed to free to avoid billing, though 512MB RAM might be tight for Chrome
    envVars:
      - key: PORT
        value: 5000
